<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lec.spring.repository.BoardRepository">
    <insert id="save" flushCache="true" keyProperty="id" parameterType="com.lec.spring.domain.Board" useGeneratedKeys="true">
        insert into post
        (title, content, type)
        values
            (#{title},#{content},#{type})
    </insert>
    <select id="findAll" parameterType="com.lec.spring.domain.Board">
        select
            id,
            user_id,
            type,
            title,
            createdat,
            followed_userid,
            user_id,
            post_id,
            name
        from post, user_follow, post_tag, attachment, user_tag, user
        order by desc
    </select>
    <select id="findById" parameterType="com.lec.spring.domain.Board">
        select
            id,
            user_id,
            type,
            title,
            createdat,
            followed_userid,
            user_id,
            post_id,
            name
        from post, user, user_follow, user_tag, post_tag
    </select>
    <update id="update" parameterType="com.lec.spring.domain.Board" flushCache="true">
        update post
        set title = #{title},
            content = #{content},
            createdat = #{createdat}
        where id = #{id}

        update user_tag
        set user_id = #{user_tag}
        where user_id = #{user_id}

        update post_tag
        set post_id = #{post_tag}
        where post_id = #{post_id}
    </update>

    <delete id="delete" parameterType="com.lec.spring.domain.Board" flushCache="true">
        delete
        from post where id = #{id}

    </delete>

</mapper>